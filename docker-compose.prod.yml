# ═══════════════════════════════════════════════════════════════════
# Evalco Dashboard — Production Stack (Hetzner)
#
# Services:
#   app         — Next.js dashboard (port 3000)
#   worker      — BullMQ sync worker
#   redis       — Queue backend
#   clickhouse  — Analytics data store
#   nginx       — Reverse proxy with SSL
#
# Usage:
#   docker compose -f docker-compose.prod.yml up -d
#   docker compose -f docker-compose.prod.yml logs -f
# ═══════════════════════════════════════════════════════════════════

services:
  # ─── Next.js App ───
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: evalco-app
    restart: always
    env_file: .env.production
    volumes:
      - sqlite_data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./data/evalco.db
      - REDIS_URL=redis://redis:6379
      - CLICKHOUSE_URL=http://clickhouse:8123
      - CLICKHOUSE_USER=evalco
      - CLICKHOUSE_PASSWORD=${CLICKHOUSE_PASSWORD:-evalco_prod}
      - CLICKHOUSE_DATABASE=evalco
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/api/health').then(r => { if (!r.ok) process.exit(1) })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          memory: 1024M
          cpus: '1.0'
    depends_on:
      redis:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    networks:
      - evalco-net

  # ─── Sync Worker ───
  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    container_name: evalco-worker
    restart: always
    env_file: .env.production
    volumes:
      - sqlite_data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./data/evalco.db
      - REDIS_URL=redis://redis:6379
      - CLICKHOUSE_URL=http://clickhouse:8123
      - CLICKHOUSE_USER=evalco
      - CLICKHOUSE_PASSWORD=${CLICKHOUSE_PASSWORD:-evalco_prod}
      - CLICKHOUSE_DATABASE=evalco
      - WORKER_CONCURRENCY=3
    healthcheck:
      test: ["CMD", "node", "-e", "setTimeout(() => process.exit(0), 100)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 768M
          cpus: '0.75'
    depends_on:
      redis:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    networks:
      - evalco-net

  # ─── Redis (Queue Backend) ───
  redis:
    image: redis:7-alpine
    container_name: evalco-redis
    restart: always
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy noeviction --auto-aof-rewrite-percentage 100 --auto-aof-rewrite-min-size 64mb
    volumes:
      - redis_data:/data
    deploy:
      resources:
        limits:
          memory: 384M
          cpus: '0.25'
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - evalco-net

  # ─── ClickHouse (Analytics) ───
  clickhouse:
    image: clickhouse/clickhouse-server:24.3
    container_name: evalco-clickhouse
    restart: always
    deploy:
      resources:
        limits:
          memory: 2048M
          cpus: '1.5'
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./scripts/clickhouse-init:/docker-entrypoint-initdb.d
    environment:
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 1
      CLICKHOUSE_USER: evalco
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-evalco_prod}
      CLICKHOUSE_DB: evalco
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    healthcheck:
      test: [ "CMD", "clickhouse-client", "--user", "evalco", "--password", "${CLICKHOUSE_PASSWORD:-evalco_prod}", "--query", "SELECT 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - evalco-net

  # ─── Nginx (Reverse Proxy + SSL) ───
  nginx:
    image: nginx:alpine
    container_name: evalco-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - certbot_data:/var/www/certbot:ro
      - certbot_certs:/etc/letsencrypt:ro
    depends_on:
      - app
    networks:
      - evalco-net

  # ─── Certbot (SSL Certificates) ───
  certbot:
    image: certbot/certbot
    container_name: evalco-certbot
    volumes:
      - certbot_data:/var/www/certbot
      - certbot_certs:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

volumes:
  sqlite_data:
  redis_data:
  clickhouse_data:
  certbot_data:
  certbot_certs:


networks:
  evalco-net:
    driver: bridge
